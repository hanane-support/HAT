:80 {
    # HATB (Bot/Admin) -> 9000 포트
    handle / {
        reverse_proxy 127.0.0.1:9000
    }

    # HATBS (Generator) -> 8000 포트
    handle /generator* {
        reverse_proxy 127.0.0.1:8000
    }
}